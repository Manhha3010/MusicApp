import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    width: 327,
    height: 44,
    backgroundColor: '#0085FF',
    borderRadius: 22,
  },
  btnText: {
    textAlign: 'center',
    color: '#fff',
    fontWeight: '700',
    lineHeight: 44,
    fontSize: 16,
  },
});
